import AdminPanel from '@/components/AdminPanel';

export const metadata = {
  title: 'Admin Control Panel - Account Reviews & Circuit Management',
  description: 'Review customer accounts, accept or reject registrations, and manage unique Circuit ID assignments.'
};

export default function AdminPage() {
  return <AdminPanel />;
}
